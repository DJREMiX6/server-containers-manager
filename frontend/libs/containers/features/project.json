{
  "name": "containers-features",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/containers/features/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["cope:containers", "type:features"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/containers/features"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
