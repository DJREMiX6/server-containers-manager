{
  "name": "namespaces-store",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/namespaces/store/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:namespaces", "type:store"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/namespaces/store"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
