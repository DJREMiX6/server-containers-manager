{
  "name": "namespaces-features",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/namespaces/features/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:namespaces", "type:features"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "coverage/libs/namespaces/features"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
